version: "3"
services:
  front-admin:
    container_name: front-admin
    build:
      dockerfile: Dockerfile
      context: .
    ports:
      - "80:80"
    environment:
      - TARGET_DOMAIN=http://localhost:8090
#    volumes:
#      - /node_modules
#      - ./:/usr/src/app
    stdin_open: true

  core-admin:
    container_name: core-admin
    build:
      dockerfile: ../core/Dockerfile2
      context: ../core
    ports:
      - "8090:8090"
#    volumes:
#      - /app/node_modules
#      - ./backend:/app
#    environment:
#      MYSQL_HOST: mysql
#      MYSQL_USER: root
#      MYSQL_ROOT_PASSWORD: johnahn777
#      MYSQL_DATABASE: myapp
#      MYSQL_PORT: 3306
#
#
#  nginx:
#    restart: always
#    build:
#      dockerfile: ../Dockerfile
#      context: ./nginx
#    ports:
#      - "3000:80"